PROGRAM ValveLogic
VAR
	ON : BOOL; (**)
	TripSeverity : INT; (**)
	Voltage : INT := 110; (**)
END_VAR
IMPLEMENTATION
	ON := TripSeverity = 1 AND Voltage <= 100 OR ON AND TripSeverity = 1;

END_IMPLEMENTATION